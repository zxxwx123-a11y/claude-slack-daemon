# secrets & local config — NEVER commit
.env
config.toml

# runtime state
.claude-slack-daemon/
work/
sessions.json
*.log

# python
venv/
__pycache__/
*.pyc
.DS_Store

# demo build artifacts — keep only the final gif in the repo
demo/frames/
demo/contact.png
demo/hero.html
